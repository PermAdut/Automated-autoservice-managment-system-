# ==============================================================================
# AutoService Management System — Frontend Environment Variables
# ==============================================================================
# Copy this file to .env and fill in your values.
# All variables MUST start with VITE_ to be exposed to the browser.
# Never put secrets here — these values are bundled into the JavaScript output.
# ==============================================================================


# ------------------------------------------------------------------------------
# API Configuration
# ------------------------------------------------------------------------------
# Backend server URL. For production point to your domain.
VITE_API_URL=http://localhost:3000

# Application title used as fallback before tenant branding is loaded.
VITE_APP_NAME=АвтоСервис

# Google Analytics / Measurement ID (optional)
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry DSN for frontend error tracking (optional)
# VITE_SENTRY_DSN=https://xxx@sentry.io/xxx
